

let initState = {
    temperature: 24
}

let weatherInfoReducer = (state = initState, action) => {
    switch (action.type){

        default:
            return state;
    }
}



export default weatherInfoReducer;